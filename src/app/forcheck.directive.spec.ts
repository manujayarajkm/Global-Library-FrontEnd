import { ForcheckDirective } from './forcheck.directive';

describe('ForcheckDirective', () => {
  it('should create an instance', () => {
    const directive = new ForcheckDirective();
    expect(directive).toBeTruthy();
  });
});
