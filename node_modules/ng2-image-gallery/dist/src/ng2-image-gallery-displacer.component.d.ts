import { TemplateRef, ViewContainerRef, OnDestroy, AfterViewInit } from '@angular/core';
import { TemplatePortal, Overlay } from '@angular/material';
export declare class DisplacerPortalDirective extends TemplatePortal<any> {
    constructor(templateRef: TemplateRef<any>, viewContainerRef: ViewContainerRef);
}
export declare class DisplacerComponent implements OnDestroy, AfterViewInit {
    private _overlay;
    private _config;
    private _portal;
    private _overlayRef;
    constructor(_overlay: Overlay);
    ngOnDestroy(): void;
    ngAfterViewInit(): void;
}
